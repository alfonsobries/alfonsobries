# Base URL of the Laravel API, including the `/api` suffix.
# Simulators reach the host over localhost; physical devices need the LAN IP
# (the `start-mobile.sh` script fills this in automatically).
EXPO_PUBLIC_API_URL=http://localhost:8000/api

# Sign in with Apple on iOS needs no client-side secret — it uses the native
# bundle identifier. Credential setup lives in docs/apple-sign-in.md.

# Live updates over websockets (the shared Reverb server). The key matches
# REVERB_APP_KEY on the API; leave it empty to fall back to polling.
EXPO_PUBLIC_REVERB_KEY=
EXPO_PUBLIC_REVERB_HOST=ws.vexilo.com
EXPO_PUBLIC_REVERB_PORT=443
EXPO_PUBLIC_REVERB_SCHEME=https

# Shared secret that signs the OTA-published webhook (must match EAS_NOTIFY_SECRET in api/.env)
EAS_NOTIFY_SECRET=
# Where the publish script announces a new update (defaults to the production API)
# OTA_NOTIFY_URL=https://api.alfonsobries.com/api/ota/published
