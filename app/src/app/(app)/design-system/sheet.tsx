import { View } from 'react-native';

import { SheetDemoContent } from '@/components/SheetDemoContent';

export default function Sheet() {
  return (
    <View className="flex-1 bg-background">
      <SheetDemoContent />
    </View>
  );
}
